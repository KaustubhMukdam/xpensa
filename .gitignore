# ─── Python / Backend ─────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
build/
dist/
*.egg-info/
.eggs/
*.egg

# Environment variables — NEVER commit these
backend/.env
backend/.env.local
backend/.env.*.local

# Alembic
# (keep alembic/versions/ tracked — those are migration files)

# pytest
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# mypy / ruff / pyright
.mypy_cache/
.ruff_cache/

# ─── Node / Frontend ──────────────────────────────
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.next/
frontend/out/
frontend/build/

# Environment variables
frontend/.env
frontend/.env.local
frontend/.env.*.local

# Vite cache
frontend/.vite/

# TypeScript build info
*.tsbuildinfo

# ─── IDE / OS ─────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ─── Docker ───────────────────────────────────────
# (keep docker-compose.yml tracked, but not override files)
docker-compose.override.yml